version: "3.9"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/app.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
